// Example configuration file
// Rename this file to config.env.js and fill in your actual values

let config = {
    botToken: 'your_bot_token_here',
    chatId: 'your_chat_id_here'
};

window.config = config;